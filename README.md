# 🛠️ Шарашкина Контора — Лендинг для ремонтной бригады

Современный одностраничный сайт (SPA) для строительно-ремонтной компании, разработанный на **Vue 3**. Проект включает в себя презентацию услуг, интерактивный прайс-лист, портфолио работ и форму обратной связи.

## 🌐 Архитектура проекта

Проект состоит из двух частей:
- **Frontend**: Vue 3 SPA приложение (`vue-project/`)
- **Backend**: REST API на Python с FastAPI (`src/`)

## 🚀 Особенности

*   **Архитектура:** Построен на Vue 3 (Composition API) + Vite.
*   **Навигация:** Реализована через динамические компоненты (Dynamic Components) без перезагрузки страницы.
*   **Интерфейс:**
    *   **Главная:** Hero-секция с pixel-art стилистикой, блок преимуществ и контакты с наложением на карту.
    *   **Расценки:** Интерактивный аккордеон для удобного просмотра цен.
    *   **Портфолио:** Карточки выполненных работ с детальным описанием и отзывами.
*   **Модальное окно:** Глобальный компонент формы заявки, доступный с любого экрана.
*   **Адаптивность:** Полная поддержка мобильных устройств и десктопов.
*   **Полная интеграция с бэкендом:** Заявки сохраняются в PostgreSQL базе данных.
*   **Портфолио с фото:** Примеры работ с фотографиями и списками использованных услуг.

## 🏗️ Структура базы данных

### Таблицы и связи
```sql
-- 1. УСЛУГИ (services)
-- Хранит все доступные услуги с ценами
id | name (наименование) | unit (ед. измерения) | price (цена за единицу)

-- 2. РАБОТЫ (works)  
-- Примеры выполненных работ с фото
id | photo_url (фото) | square (квадратура) | price (общая цена) | description (описание)

-- 3. ЗАЯВКИ (repair_requests)
-- Заявки от клиентов
id | name (имя) | phone_number (телефон) | created_at (дата создания)

-- 4. СВЯЗЬ РАБОТЫ-УСЛУГИ (work_service_association)
-- Связь многие-ко-многим между работами и услугами
work_id | service_id | quantity (количество услуги в работе)
```

### Ключевые особенности базы данных:
- **Одна работа → Много услуг**: Каждая работа в портфолио содержит несколько услуг из каталога
- **Услуги переиспользуются**: Одна и та же услуга может быть в разных работах
- **Количества услуг**: Для каждой услуги в работе указывается количество (например, 15 м² плитки)

## 🛠️ Технологический стек

### Frontend
*   [Vue.js 3](https://vuejs.org/) — основной фреймворк.
*   [Vite](https://vitejs.dev/) — сборщик проекта.
*   **CSS3** — Scoped стили, Flexbox, Grid Layout.
*   **HTML5 / SVG** — семантическая верстка и векторная графика.

### Backend
*   **FastAPI** — современный, быстрый веб-фреймворк для Python
*   **SQLAlchemy** — ORM для работы с базой данных
*   **PostgreSQL** — реляционная база данных
*   **Docker & Docker Compose** — контейнеризация и оркестрация
*   **Pydantic** — валидация данных и сериализация

## 📦 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/avocadonet/website-repair.git
cd website-repair
```

### 2. Запуск бэкенда (Backend API)

#### Способ A: Через Docker Compose (рекомендуется)
```bash
# Запуск всех сервисов (PostgreSQL + FastAPI)
docker-compose up -d

# Заполнение базы тестовыми данными (включая работы с услугами)
docker-compose exec backend python populate_database.py

# Проверка работы API
curl http://localhost:8000/
# Должен вернуть: {"message":"Repair Service API","version":"2.0.0",...}
```

#### Способ B: Ручной запуск
```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск PostgreSQL
docker-compose up -d postgres

# Запуск FastAPI приложения
python src/main.py
```

### 3. Запуск фронтенда (Frontend Vue.js)
```bash
# Переходим в папку с фронтендом
cd vue-project

# Устанавливаем зависимости
npm install

# Запускаем в режиме разработки
npm run dev
```

### 4. Проверка работы
- **Frontend:** `http://localhost:5173`
- **Backend API:** `http://localhost:8000`
- **API документация (Swagger UI):** `http://localhost:8000/docs`
- **Автоматическая документация (ReDoc):** `http://localhost:8000/redoc`
- **Статические файлы (фото):** `http://localhost:8000/static/photos/`

## 🗄️ Подробная структура данных

### 1. Услуги (services)
Каталог всех доступных услуг компании:
```json
{
  "id": 1,
  "name": "Укладка керамической плитки",
  "unit": "м²", 
  "price": 1200
}
```

### 2. Работы (works) 
Примеры выполненных работ с фотографиями:
```json
{
  "id": 1,
  "photo_url": "/static/photos/bathroom_remodel_1.jpg",
  "square": 6.5,
  "price": 85000,
  "description": "Ремонт ванной комнаты с заменой сантехники",
  "services": [
    {
      "id": 1,
      "name": "Демонтаж старой сантехники",
      "unit": "шт",
      "price": 800,
      "quantity": 1.0
    },
    {
      "id": 2, 
      "name": "Установка унитаза",
      "unit": "шт",
      "price": 2500,
      "quantity": 1.0
    }
  ]
}
```

### 3. Заявки (repair_requests)
Заявки от клиентов на ремонт:
```json
{
  "id": 1,
  "name": "Иван Иванов",
  "phone_number": "79161234567",
  "created_at": "2024-01-15T10:30:00"
}
```

## 🔌 API Эндпоинты

### Основные эндпоинты
| Метод | Путь | Описание | Пример тела запроса |
|-------|------|----------|---------------------|
| GET | `/` | Информация о API | - |
| GET | `/health` | Проверка здоровья | - |
| **РАБОТЫ** | | | |
| GET | `/api/works/` | Все работы с услугами | - |
| GET | `/api/works/{id}` | Конкретная работа | - |
| POST | `/api/works/` | Создать работу | `{"photo_url": "...", "square": 6.5, "price": 85000, "service_ids": [1,2], "quantities": [1,1]}` |
| POST | `/api/works/{id}/services/{service_id}` | Добавить услугу к работе | `?quantity=2` |
| **УСЛУГИ** | | | |
| GET | `/api/services/` | Все услуги | - |
| GET | `/api/services/{id}` | Конкретная услуга | - |
| **ЗАЯВКИ** | | | |
| POST | `/api/repair-requests/` | Создать заявку | `{"name": "Иван", "phone_number": "79161234567"}` |
| GET | `/api/repair-requests/` | Все заявки | - |
| GET | `/api/repair-requests/{id}` | Конкретная заявка | - |

### Примеры запросов

**Создание заявки:**
```bash
curl -X POST "http://localhost:8000/api/repair-requests/" \
  -H "Content-Type: application/json" \
  -d '{"name": "Иван Иванов", "phone_number": "79161234567"}'
```

**Получение работ с услугами:**
```bash
curl http://localhost:8000/api/works/
```

**Добавление услуги к существующей работе:**
```bash
curl -X POST "http://localhost:8000/api/works/1/services/3?quantity=2.5"
```

## 🏗️ Архитектура бэкенда

```
src/
├── core/                    # Ядро системы (бизнес-логика)
│   ├── entities/           # Сущности (RepairRequest, Service, Work)
│   └── ports/              # Интерфейсы (ABC)
│
├── infrastructure/         # Внешние компоненты
│   ├── database/          # Настройка БД, модели, репозитории
│   └── web/              # Веб-слой (FastAPI, эндпоинты)
│
├── application/           # Слой приложения
│   └── use_cases/        # Use Cases (бизнес-операции)
│
└── main.py               # Точка входа
```

### Ключевые особенности архитектуры:
- **Чистая архитектура**: Разделение на слои для легкого тестирования и поддержки
- **Репозитории**: Абстракция доступа к данным
- **Связь многие-ко-многим**: Правильная реляционная модель для работ и услуг
- **JSON API**: Современный REST API с полной документацией

## 🔧 Команды для разработки

### Docker команды
```bash
# Сборка и запуск
docker-compose up -d --build

# Просмотр логов бэкенда
docker-compose logs -f backend

# Просмотр логов PostgreSQL
docker-compose logs -f postgres

# Остановка всех сервисов
docker-compose down

# Полная очистка (удаление volumes)
docker-compose down -v

# Запуск консоли PostgreSQL
docker-compose exec postgres psql -U repair_user -d repair_db
```

### Управление базой данных
```bash
# Пересоздание таблиц и заполнение тестовыми данными
docker-compose exec backend python populate_database.py

# Быстрое заполнение (минимальные данные)
docker-compose exec backend python populate_quick.py

# Просмотр структуры базы
docker-compose exec postgres psql -U repair_user -d repair_db -c "\dt"
```

### Работа с данными
```bash
# Просмотр всех работ
curl http://localhost:8000/api/works/ | jq '.[] | {id, description, services_count: .services | length}'

# Просмотр всех услуг
curl http://localhost:8000/api/services/ | jq '.[] | {id, name, price}'

# Проверка связей в базе
docker-compose exec postgres psql -U repair_user -d repair_db -c "
SELECT w.id, w.description, s.name, wsa.quantity
FROM works w
JOIN work_service_association wsa ON w.id = wsa.work_id
JOIN services s ON wsa.service_id = s.id
ORDER BY w.id;
"
```

### Frontend команды
```bash
# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Превью собранного проекта
npm run preview

# Проверка кода ESLint
npm run lint
```

## 📸 Работа с фотографиями

### Структура фотографий
```
website-repair/
├── photos/                    # Исходные фото (опционально)
│   ├── bathroom_remodel_1.jpg
│   ├── kitchen_remodel_1.jpg
│   └── ...
├── src/static/photos/        # Фото для веб-доступа (создается автоматически)
└── ...
```

### Добавление реальных фото
1. Поместите фото в папку `photos/` в корне проекта
2. Запустите скрипт заполнения базы
3. Фото будут доступны по: `http://localhost:8000/static/photos/имя_файла.jpg`

### Формат данных в базе
```python
# В таблице works поле photo_url содержит путь к фото:
photo_url = "/static/photos/bathroom_remodel_1.jpg"
```

## 🌐 Интеграция Frontend-Backend

### Vue компонент для отправки заявок
```vue
<template>
  <form @submit.prevent="submitRequest">
    <input v-model="name" placeholder="Имя">
    <input v-model="phone" placeholder="Телефон">
    <button>Отправить</button>
  </form>
</template>

<script>
export default {
  data() {
    return {
      name: '',
      phone: ''
    }
  },
  methods: {
    async submitRequest() {
      const response = await fetch('http://localhost:8000/api/repair-requests/', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          name: this.name,
          phone_number: this.phone.replace(/\D/g, '')
        })
      })
      
      if (response.ok) {
        alert('Заявка отправлена!')
      }
    }
  }
}
</script>
```

### Получение работ для портфолио
```javascript
// Пример получения работ с фронтенда
async function loadPortfolio() {
  const response = await fetch('http://localhost:8000/api/works/');
  const works = await response.json();
  
  // Каждая работа содержит:
  // - photo_url (путь к фото)
  // - square (площадь)
  // - price (стоимость)
  // - services[] (массив услуг с количествами)
  
  return works;
}
```

### CORS настройки
Бэкенд настроен на работу с фронтендом на портах:
- `http://localhost:5173` (Vite dev server)
- `http://localhost:3000` (React)
- `http://127.0.0.1:5173`

## 📊 Администрирование и мониторинг

### Мониторинг
```bash
# Статус всех контейнеров
docker-compose ps

# Логи в реальном времени
docker-compose logs -f

# Использование ресурсов
docker stats
```

### База данных
```bash
# Подключение к PostgreSQL
docker-compose exec postgres psql -U repair_user -d repair_db

# Полезные запросы:
\dt                          # Показать все таблицы
SELECT COUNT(*) FROM works;  # Количество работ
SELECT * FROM services ORDER BY price DESC;  # Услуги по цене

# Просмотр связей многие-ко-многим
SELECT 
  w.description as работа,
  s.name as услуга,
  wsa.quantity as количество,
  s.unit as ед_измерения
FROM works w
JOIN work_service_association wsa ON w.id = wsa.work_id
JOIN services s ON wsa.service_id = s.id
ORDER BY w.id, s.name;

\q  # Выход
```

### Проверка данных через API
```bash
# Количество записей в каждой таблице
curl -s http://localhost:8000/api/works/ | jq length
curl -s http://localhost:8000/api/services/ | jq length
curl -s http://localhost:8000/api/repair-requests/ | jq length

# Детальная информация о работе
curl -s http://localhost:8000/api/works/1 | jq '{
  id,
  description,
  square,
  price,
  services_count: .services | length,
  total_services_price: [.services[] | .price * .quantity] | add
}'
```

## 🚀 Деплой в продакшен

### Production Dockerfile
```dockerfile
FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y \
    libpq5 \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/ ./src/

RUN useradd --create-home --shell /bin/bash app && \
    chown -R app:app /app
USER app

CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000", "--no-access-log"]
```

### Рекомендации для продакшена
1. **Измените дефолтные пароли** в `docker-compose.yml`
2. **Настройте HTTPS** через reverse proxy (Nginx)
3. **Добавьте авторизацию** для админских эндпоинтов
4. **Настройте бэкапы** базы данных
5. **Используйте переменные окружения** для секретов
6. **Настройте мониторинг** и логирование
7. **Используйте CDN** для статических файлов (фото)

## 🐛 Устранение неполадок

### Частые проблемы

1. **"Connection refused" при подключении к PostgreSQL**
   ```bash
   docker-compose restart postgres
   docker-compose logs -f postgres
   ```

2. **CORS ошибки во фронтенде**
   - Убедитесь что фронтенд запущен на порту из списка разрешенных
   - Проверьте настройки CORS в `src/infrastructure/web/app.py`

3. **API не отвечает**
   ```bash
   # Проверьте статус контейнеров
   docker-compose ps
   
   # Просмотрите логи
   docker-compose logs -f backend
   
   # Проверьте доступность API
   curl http://localhost:8000/health
   ```

4. **Проблемы со связями многие-ко-многим**
   ```bash
   # Проверьте структуру связей
   docker-compose exec backend python -c "
   from sqlalchemy.orm import Session
   from infrastructure.database.base import SessionLocal
   
   db = SessionLocal()
   try:
       result = db.execute('SELECT COUNT(*) FROM work_service_association').first()
       print(f'Связей в базе: {result[0]}')
   finally:
       db.close()
   "
   ```

5. **Фото не отображаются**
   - Проверьте что папка `static/photos/` существует
   - Убедитесь что в базе правильные пути к фото
   - Проверьте доступность: `curl http://localhost:8000/static/photos/bathroom_remodel_1.jpg`

### Логирование
```bash
# Просмотр всех логов
docker-compose logs

# Логи в реальном времени
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f postgres

# Очистка логов (при необходимости)
docker-compose logs --tail=0 -f
```

## 📚 Документация

### API документация
- **Swagger UI:** `http://localhost:8000/docs` (интерактивная документация)
- **ReDoc:** `http://localhost:8000/redoc` (альтернативный вид)
- **OpenAPI схема:** `http://localhost:8000/openapi.json`

### Примеры использования API

**Получение работ для портфолио:**
```javascript
// Frontend (Vue/React)
fetch('http://localhost:8000/api/works/')
  .then(response => response.json())
  .then(works => {
    // works содержит массив работ с услугами
    works.forEach(work => {
      console.log(`${work.description}: ${work.services.length} услуг`);
    });
  });
```

**Создание новой работы (админка):**
```bash
curl -X POST "http://localhost:8000/api/works/" \
  -H "Content-Type: application/json" \
  -d '{
    "photo_url": "/static/photos/new_work.jpg",
    "square": 15.5,
    "price": 120000,
    "description": "Новый ремонт",
    "service_ids": [1, 3, 5],
    "quantities": [1, 2.5, 10]
  }'
```

### Полезные ссылки
- [Vue 3 документация](https://vuejs.org/guide/introduction.html)
- [Vite документация](https://vitejs.dev/guide/)
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)
- [Docker документация](https://docs.docker.com/)
- [PostgreSQL документация](https://www.postgresql.org/docs/)

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте логи:** `docker-compose logs -f`
2. **Посмотрите документацию:** `http://localhost:8000/docs`
3. **Проверьте структуру базы:**
   ```bash
   docker-compose exec backend python -c "
   from sqlalchemy.orm import Session
   from infrastructure.database.base import SessionLocal
   
   db = SessionLocal()
   try:
       tables = ['repair_requests', 'services', 'works', 'work_service_association']
       for table in tables:
           result = db.execute(f'SELECT COUNT(*) FROM {table}').first()
           print(f'{table}: {result[0]} записей')
   finally:
       db.close()
   "
   ```
4. **Создайте issue** в репозитории с описанием проблемы

---

**🎉 Проект готов к использованию!** 

Система включает:
- ✅ **Заявки на ремонт** с сохранением в базе
- ✅ **Каталог услуг** с ценами и единицами измерения
- ✅ **Портфолио работ** с фотографиями
- ✅ **Связи многие-ко-многим** между работами и услугами
- ✅ **Полную документацию API** (Swagger/ReDoc)
- ✅ **Контейнеризацию** (Docker) для легкого развертывания

Запустите и проверьте: `http://localhost:8000/docs`